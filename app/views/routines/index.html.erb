<h1>Routines#index</h1>
<% @routines.each do |r| %>
<%= r.name %> <br>
    <% end %>

<% if @product %>
    <%= link_to "Create New Routine", new_product_routine_path(@product) %>
<% else %>
    <%= link_to "Create New Routine", new_routine_path %>

<% end %>

